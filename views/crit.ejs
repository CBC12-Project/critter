<!DOCTYPE html>
<html lang="en">

<%- include('partials/head.ejs',{page:{name:'Home'}}) %>  
<!-- HEAD EJS -->
<body>
    
    <!-- grid container for overall layout -->
    <div class="container"> 
        <div class="logo">
            <div class="logo">
            <img id="crit-logo" src="https://img.icons8.com/ios-glyphs/60/000000/bug--v1.png"/>
            <br><br>
            
        </div> 
    </div>
        <div class="side-menu">
            <%-include('partials/sidebar') %>
        </div>
        
        <!-- grid section that shows timeline of crits -->
        <div class="timeline">
            <!-- Send a Crit -->
            <form action="/crit" method="post">
                <input type="text" id="create-crit" name="createCrit" >
                <button id="create-crit-btn">CRIT!</button>
            </form>
            
            <%- include('partials/single-crit', { user: user, crit: crit }); %>    
                <!-- SINGLE CRIT EJS -->
                <form action="/crit/<%= crit.id %>" method="post">
                    <input type="text" id="reply-crit" name="replyCrit" >
                    <button id="reply-crit-btn">Reply!</button>
                </form>
            
        </div>
        
        <%- include('partials/search') %>

		<div class="results">
            <%# include('/single-crit', {user: { display_name: 'Andrew', picture: src="", username: '@AndrewB'}, crit:{created_on: '', message:'Hello', likes: 1, replies:'2' }}); %> 
            <!-- template for crits and card design  -->
            
            <!-- reply: template for replies to crits -->
            
            
            <%# TODO(implement replies) %>
            <%# include('partials/single-crit', { user: user, crit: crit }); %> 

			<!-- 
            <div class="crit card reply reply-text">
                <div class="divider">
                    <div class="crit-pfp"></div>
                    <input type="text" class="reply-textbox" placeholder="Write reply">
                </div>
            </div>
			-->
        </div>

    </div>
</body>
</html>